spring:
  application:
    name: relation
  datasource:
    username: user
    password: password
    url: jdbc:postgresql://localhost:5434/relations
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format-sql: true
      show-sql: true