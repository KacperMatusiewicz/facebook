server:
  port: 8125
spring:
  application:
    name: search
eureka:
  client:
    service-url:
      defaultZone: "http://eureka:8761/eureka"
    register-with-eureka: true
    fetch-registry: true
elasticsearch:
  host: elasticsearch:9200